version: '3.8'

services:
  resume-analyzer:
    build: .
    container_name: resume-analyzer-app
    ports:
      - "8501:8501"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: unless-stopped
